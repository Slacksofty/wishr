services:
  wishr:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - wishr-data:/app/data
    environment:
      DATABASE_URL: sqlite:/app/data/wishr.db
      LEPTOS_SITE_ADDR: 0.0.0.0:3000
      LEPTOS_SITE_ROOT: /app/site
    restart: unless-stopped

volumes:
  wishr-data:
